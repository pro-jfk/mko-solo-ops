credentials:
  system:
    domainCredentials:
      - credentials:
          - string:
              id: "harborn_kubernetes"
              scope: GLOBAL
              secret: "${KUBERNETES_SA_TOKEN}"